name: Pull Request Stats run

on:
  pull_request:
    types: [opened]

jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - name: Run pull request stats
        uses: flowwer-dev/pull-request-stats@master
        with:
          token: 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
          organization: "piedpiper"
          period: 7
          charts: true
          disableLinks: true
          sortBy: "COMMENTS"
